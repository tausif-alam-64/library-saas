// app/(app)/dashboard/page.jsx

import { LoadingSpinner } from '@/components/ui/LoadingSpinner'

// Temporary placeholder — replaced in Phase 10
// Exists so the login redirect to /dashboard works
export default function DashboardPage() {
  return (
    <div style={{ padding: '1.5rem' }}>
      <p style={{
        fontSize: '0.875rem',
        color: '#9ca3af',
        textAlign: 'center',
        paddingTop: '2rem',
      }}>
        Dashboard coming in Phase 10.
      </p>
    </div>
  )
}